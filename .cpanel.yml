---
deployment:
  tasks:
    - /usr/local/bin/php artisan migrate --force
    - /usr/local/bin/php artisan db:seed --force
    - /usr/local/bin/php artisan optimize:clear
    - /usr/local/bin/php artisan storage:verify-public --repair
