<article class="product-card reveal"><a href="{{ route('product',$product) }}"><div class="product-thumb">@if($product->images->first())<img src="{{ asset('storage/'.$product->images->first()->path) }}" alt="{{ $product->name }}">@else<div class="product-placeholder">TROPIKAL</div>@endif</div><div class="product-body"><span class="eyebrow">{{ $product->category->name ?? '' }}</span><h3>{{ $product->name }}</h3><p>{{ $product->excerpt }}</p><strong>{{ $product->price ? number_format($product->price,2,',','.') . ' €' : 'Sob consulta' }}</strong></div></a></article>
