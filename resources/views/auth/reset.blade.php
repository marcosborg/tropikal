@extends('layouts.site') @section('content')<section class="section account-auth"><div class="container"><form class="contact-form account-card" method="post" action="{{ route('password.update') }}">@csrf<h1>Nova palavra-passe</h1>@if($errors->any())<p class="form-feedback is-error">{{ $errors->first() }}</p>@endif<input type="hidden" name="token" value="{{ $token }}"><label>Email<input type="email" name="email" value="{{ $email }}" required></label><label>Palavra-passe<input type="password" name="password" minlength="10" required></label><label>Confirmar<input type="password" name="password_confirmation" required></label><button class="btn btn-primary">Guardar</button></form></div></section>@endsection
