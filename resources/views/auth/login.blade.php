@extends('layouts.site') @section('title','Entrar | TROPIKAL') @section('content')<section class="section account-auth"><div class="container"><form class="contact-form account-card" method="post">@csrf<h1>Entrar</h1><p>Acompanhe encomendas, moradas e devoluções.</p>@if(session('status'))<p class="form-feedback is-success">{{ session('status') }}</p>@endif @if($errors->any())<p class="form-feedback is-error">{{ $errors->first() }}</p>@endif<label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><label>Palavra-passe<input type="password" name="password" required></label><label class="check-label"><input type="checkbox" name="remember" value="1"><span>Manter sessão iniciada</span></label><button class="btn btn-primary">Entrar</button><p><a href="{{ route('password.request') }}">Esqueci-me da palavra-passe</a> · <a href="{{ route('register') }}">Criar conta</a></p></form></div></section>@endsection
