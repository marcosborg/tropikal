@extends('layouts.site') @section('content')<section class="section account-auth"><div class="container"><form class="contact-form account-card" method="post">@csrf<h1>Recuperar acesso</h1>@if(session('status'))<p class="form-feedback is-success">{{ session('status') }}</p>@endif @if($errors->any())<p class="form-feedback is-error">{{ $errors->first() }}</p>@endif<label>Email<input type="email" name="email" required></label><button class="btn btn-primary">Enviar ligação</button></form></div></section>@endsection
