<h1>{{ $internal ? 'Novo pedido sujeito a confirmação' : 'Recebemos o seu pedido' }}</h1><p><strong>Referência:</strong> {{ $orderRequest->reference }}</p><p><strong>Cliente:</strong> {{ $orderRequest->name }} — {{ $orderRequest->email }} — {{ $orderRequest->phone }}</p><p><strong>Região:</strong> {{ $orderRequest->region }}</p>@if($orderRequest->shipping_address)<p><strong>Entrega:</strong> {{ $orderRequest->shipping_address['line1'] ?? '' }} {{ $orderRequest->shipping_address['line2'] ?? '' }}, {{ $orderRequest->shipping_address['postal_code'] ?? '' }} {{ $orderRequest->shipping_address['city'] ?? '' }}</p>@endif<h2>Artigos</h2><ul>@foreach($orderRequest->items as $item)<li>{{ $item->quantity }} × {{ $item->product_name }}{{ $item->variant_name ? ' — '.$item->variant_name : '' }}</li>@endforeach</ul>@if($orderRequest->subtotal !== null)<p><strong>Subtotal indicativo:</strong> {{ number_format($orderRequest->subtotal,2,',','.') }} €</p>@endif<p>Este pedido está sujeito à confirmação de preço, disponibilidade, transporte, stock e condições pela Tropikal. Não foi efetuada qualquer cobrança.</p>
