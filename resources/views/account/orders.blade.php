@extends('layouts.site') @section('content')<section class="section"><div class="container account-layout">@include('account.nav')<div><h1>As minhas encomendas</h1>@forelse($orders as $order)<a class="account-order" href="{{ route('account.order',$order) }}"><strong>{{ $order->number }}</strong><span>{{ $order->created_at->format('d/m/Y') }} · {{ number_format($order->total,2,',','.') }} € · {{ $order->status }}</span></a>@empty<p>Ainda não existem encomendas.</p>@endforelse{{ $orders->links() }}</div></div></section>@endsection
